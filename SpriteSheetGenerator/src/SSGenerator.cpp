#include "SSGenerator.h"

bool SSGenerator::generateSpriteSheets( const std::vector<QString>& filenames, const QString& folderPath, const bool automaticSize, const QSize& fixedSize )
{
	return false;
}